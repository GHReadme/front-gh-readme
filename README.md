# Frontend GH Readme

## Процесс разработки

### Создание веток

В этом проекте используется автоматическое создание веток через GitHub Actions.

#### Как это работает:

1. **Создайте Issue** в GitHub с описанием задачи
2. **Система автоматически** создаст ветку в формате: `issue/{номер-issue}`
3. **Для начала работы** переключитесь на созданную ветку:
   ```
   git fetch --all
   git checkout issue/{номер-issue}
   ```
4. **Коммиты** создаем коммит с указанием номера issue
      ```
      git commit -m "#{номер-issue} текст коммита"
      ```

## 🧱 Упрощённая архитектура FSD для Next.js

### 📁 Структура проекта

```
    src/
    ├── app/            # маршруты, layout, страницы (Next 13+)
    ├── shared/         # общие модули (UI, utils, API, config)
    ├── entities/       # бизнес-сущности (User, Product)
    ├── features/       # пользовательские фичи (Login, Search)
    ├── widgets/        # крупные блоки интерфейса (Header, Footer)
    └── pages/          # страницы (если не App Router)
 ```

### 🔹 Назначение слоёв
**app/** — маршруты, layout, SSR, сборка интерфейса.  
**widgets/** — крупные блоки, объединяющие фичи и сущности.  
**features/** — отдельные пользовательские возможности.  
**entities/** — бизнес-сущности (данные, UI, запросы).  
**shared/** — общие модули, без бизнес-логики.

### 🔸 Внутренняя структура модуля
```
ui/      - компоненты
model/   - состояние, запросы, типы
lib/     - утилиты, форматтеры
api/     - запросы к серверу
config/  - константы и настройки
index.ts - публичный API модуля
```

### 🔗 Правила зависимостей
| Уровень     | Может зависеть от |
|--------------|------------------|
| app          | widgets, features, entities, shared |
| widgets      | features, entities, shared |
| features     | entities, shared |
| entities     | shared |
| shared       | — (ни от кого) |

### ✅ Принципы
- Каждый модуль автономен.  
- Импорты — только через `index.ts`.  
- Переиспользуемый код — в `shared`.  
- Новая логика — в слое по смыслу.

