/**
 * Универсальный helper: любые настройки + флаг активности секции.
 *
 * Пример:
 * type AboutSection = WithActive<{ shortBio: string }>;
 * const about: AboutSection = {
 *   active: true,
 *   shortBio: 'Senior frontend dev...',
 * };
 */
export type WithActive<T> = T & {
  /** Включена ли секция в итоговом README */
  active: boolean;
};

/** Типы контактов / ссылок, которые можно показать в профиле */
export type ContactType =
  | 'email'
  | 'github'
  | 'linkedin'
  | 'telegram'
  | 'x'
  | 'website'
  | 'youtube'
  | 'devto'
  | 'medium'
  | 'other';

/**
 * Один контакт/ссылка.
 *
 * Пример:
 * {
 *   type: 'telegram',
 *   label: 'Telegram',
 *   url: 'https://t.me/your_handle'
 * }
 */
export interface ContactItem {
  /** Тип контакта — можно использовать для выбора иконки */
  type: ContactType;

  /**
   * Надпись на кнопке/ссылке.
   * Примеры:
   * - 'Email'
   * - 'Telegram'
   * - 'Portfolio'
   */
  label?: string;

  /**
   * Сама ссылка.
   * Примеры:
   * - 'mailto:you@example.com'
   * - 'https://github.com/your-username'
   * - 'https://t.me/your_handle'
   */
  url: string;
}

/** Категория технологии — удобно для группировки в README */
export type StackCategory =
  | 'language'
  | 'frontend'
  | 'backend'
  | 'mobile'
  | 'devops'
  | 'testing'
  | 'database'
  | 'tool'
  | 'other';

/**
 * Один элемент техстека.
 *
 * Пример:
 * {
 *   name: 'React',
 *   category: 'frontend',
 *   level: 'expert',
 *   icon: 'react' // имя иконки, emoji, CSS-класс — как решишь
 * }
 */
export interface StackItem {
  /** Название технологии: 'TypeScript', 'React', 'Jest' */
  name: string;

  /** Категория — влияет только на презентацию */
  category: StackCategory;

  /**
   * Условный уровень владения.
   * Можно использовать для прогресс-баров/бейджей.
   */
  level?: 'beginner' | 'intermediate' | 'advanced' | 'expert';

  /**
   * Иконка / ключ иконки.
   * Примеры:
   * - '🟦'
   * - 'react'
   * - 'logos:typescript-icon'
   */
  icon?: string;
}

/**
 * Группа технологий — будет отдельным подразделом в README.
 *
 * Пример:
 * {
 *   title: 'Frontend',
 *   items: [...StackItem]
 * }
 */
export interface StackGroup {
  /** Заголовок: 'Languages', 'Frontend', 'Testing & Tools' */
  title: string;

  /** Список технологий в этой группе */
  items: StackItem[];
}

/**
 * Проект, который ты хочешь подсветить в README.
 *
 * Пример:
 * {
 *   id: 'readme-builder',
 *   name: 'GitHub README Builder',
 *   description: 'Visual config-driven README generator.',
 *   repoUrl: 'https://github.com/your-username/readme-builder',
 *   demoUrl: 'https://readme-builder.dev',
 *   tech: ['React', 'TypeScript', 'Vite'],
 *   highlight: true
 * }
 */
export interface FeaturedProject {
  /** Внутренний id (для React key, сортировки и т.д.) */
  id: string;

  /** Название проекта / репозитория */
  name: string;

  /** Короткое описание (1–2 предложения) */
  description: string;

  /** Ссылка на репозиторий GitHub (если есть) */
  repoUrl?: string;

  /** Ссылка на демо/production-версию (если есть) */
  demoUrl?: string;

  /** Стек по проекту — свободный массив строк */
  tech: string[];

  /**
   * Флаг "особо важный" проект.
   * Пример: true для pet-project, которым гордишься.
   */
  highlight?: boolean;

  /**
   * Твоя роль в проекте.
   * Примеры:
   * - 'Solo developer'
   * - 'Frontend lead'
   * - 'Contributor'
   */
  role?: string;

  /**
   * Краткий период/таймлайн.
   * Примеры:
   * - '2023 — now'
   * - '2022'
   */
  period?: string;
}

/**
 * Настройки для GitHub-статистики (карточки и т.д.)
 *
 * Здесь только логические флаги + опционально кастомный username.
 */
export interface GithubStatsSettings {
  /** Показать общую overview-карточку (commits, stars, etc.) */
  showOverviewCard: boolean;

  /** Показать карточку с топ-языками */
  showTopLangs: boolean;

  /** Показать streak-карточку */
  showStreak: boolean;

  /**
   * Тема для карточек.
   * Примеры:
   * - 'dark'
   * - 'light'
   * - 'transparent'
   */
  theme?: 'light' | 'dark' | 'transparent';

  /**
   * Кастомный username для генераторов stats.
   * Можно переопределить profile.username.
   */
  customUsername?: string;
}

/**
 * Один элемент "активности"/статуса.
 * Можно выводить как чек-лист или badges.
 *
 * Пример:
 * {
 *   label: 'Open to remote roles',
 *   value: true
 * }
 */
export interface ActivityFlag {
  /** Текст/лейбл: 'Open to work', 'Mentoring', 'Open source' */
  label: string;

  /** Включен ли флаг */
  value: boolean;

  /**
   * Детализация/комментарий.
   * Примеры:
   * - 'Frontend roles (React/TS), Europe timezones'
   * - 'Happy to review PRs in OSS projects'
   */
  note?: string;
}

/**
 * Ссылка на контент (статья, видео, пост).
 *
 * Пример:
 * {
 *   title: 'Scaling frontends with feature-sliced design',
 *   url: 'https://dev.to/...',
 *   platform: 'dev.to',
 *   publishedAt: '2024-05-01'
 * }
 */
export interface ContentLink {
  /** Заголовок материала */
  title: string;

  /** Прямая ссылка на материал */
  url: string;

  /**
   * Платформа/тип.
   * Примеры:
   * - 'dev.to'
   * - 'medium'
   * - 'youtube'
   * - 'personal-blog'
   */
  platform: string;

  /**
   * Дата публикации в ISO-формате.
   * Пример:
   * - '2024-05-01'
   */
  publishedAt?: string;
}

/**
 * Главный конфиг профиля для README.
 * Всё, что нужно для типичной страницы GitHub.
 */
export interface ProfileReadmeConfig {
  // ─────────────────────────────────────────────────────────────
  // 1. Базовая информация о разработчике
  // ─────────────────────────────────────────────────────────────
  profile: {
    /**
     * GitHub username
     * Примеры:
     * - 'gaearon'
     * - 'sindresorhus'
     * - 'your-username'
     */
    username: string;

    /** Полное имя (опционально) — 'John Doe' */
    fullName?: string;

    /**
     * Роль/позиция в одно предложение.
     * Примеры:
     * - 'Senior Frontend Developer'
     * - 'Full-Stack Engineer (React / Node.js)'
     */
    role?: string;

    /**
     * Текущая компания/род занятий.
     * Примеры:
     * - 'Frontend Engineer @ AwesomeCorp'
     * - 'Freelance developer'
     * - 'Student'
     */
    company?: string;

    /**
     * Локация.
     * Примеры:
     * - 'Oslo, Norway'
     * - 'Remote'
     */
    location?: string;

    /**
     * Таймзона (если хочешь вывести в README).
     * Пример:
     * - 'Europe/Oslo'
     */
    timezone?: string;

    /**
     * Основной стек в виде короткой строки.
     * Примеры:
     * - 'React · TypeScript · Node.js'
     * - 'Frontend · Architecture · DX'
     */
    mainStackLine?: string;

    /**
     * Языки, на которых удобно общаться, для блока "Languages".
     * Пример: ['English', 'Russian']
     */
    languages?: string[];

    /**
     * Ссылка на аватар/фото (не обязательно).
     * Обычно можно брать GitHub-аватар.
     */
    avatarUrl?: string;
  };

  // ─────────────────────────────────────────────────────────────
  // 2. Блок "Обо мне" / About
  // ─────────────────────────────────────────────────────────────
  about: WithActive<{
    /**
     * Короткое описание в 1–3 предложения.
     * Примеры:
     * - 'Senior frontend engineer focusing on scalable React + TypeScript apps and DX.'
     * - 'I design maintainable frontend architectures and care about developer experience.'
     */
    shortBio: string;

    /**
     * Над чем сейчас работаешь.
     * Примеры:
     * - 'Currently building internal design system.'
     * - 'Working on open source tooling for React.'
     */
    currentFocus?: string;

    /**
     * Что сейчас изучаешь.
     * Примеры:
     * - 'Advanced web security and pentesting basics.'
     * - 'Performance profiling and bundle optimization.'
     */
    currentlyLearning?: string;

    /**
     * Что ищешь / открыт к чему.
     * Примеры:
     * - 'Open to senior frontend roles (remote, Europe timezones).'
     * - 'Looking for interesting open source projects to contribute to.'
     */
    lookingFor?: string;

    /**
     * Краткий список "ценностей" или тезисов.
     * Пример:
     * [
     *   'I value clean architecture and clear boundaries.',
     *   'I like refactoring and improving DX.',
     *   'I prefer testing and static typing over "move fast and break things".'
     * ]
     */
    values?: string[];
  }>;

  // ─────────────────────────────────────────────────────────────
  // 3. Контакты / ссылки
  // ─────────────────────────────────────────────────────────────
  contacts: WithActive<{
    /** Список всех контактов/ссылок для блока "Connect with me" */
    items: ContactItem[];
  }>;

  // ─────────────────────────────────────────────────────────────
  // 4. Техстек
  // ─────────────────────────────────────────────────────────────
  techStack: WithActive<{
    /**
     * Группировать ли стек по группам (StackGroup).
     * Если false — можно рендерить просто одним списком.
     */
    grouped: boolean;

    /**
     * Список групп, каждая со своим заголовком и технологиями.
     * Примеры групп:
     * - 'Languages'
     * - 'Frontend'
     * - 'Backend'
     * - 'Testing & Tools'
     */
    groups: StackGroup[];
  }>;

  // ─────────────────────────────────────────────────────────────
  // 5. Выделенные проекты
  // ─────────────────────────────────────────────────────────────
  projects: WithActive<{
    /** Список проектов для отображения в README */
    items: FeaturedProject[];

    /**
     * Ограничение по количеству отображаемых проектов.
     * Пример: 3 или 6.
     */
    maxToShow?: number;

    /**
     * Если true — сначала показывать проекты с highlight === true.
     */
    showHighlightedFirst?: boolean;
  }>;

  // ─────────────────────────────────────────────────────────────
  // 6. GitHub-статистика
  // ─────────────────────────────────────────────────────────────
  githubStats: WithActive<GithubStatsSettings>;

  // ─────────────────────────────────────────────────────────────
  // 7. Активность / статус (Open to work, OSS и т.п.)
  // ─────────────────────────────────────────────────────────────
  activity: WithActive<{
    /**
     * Набор "флагов" активности.
     * Примеры:
     * - 'Open to work'
     * - 'Open source contributor'
     * - 'Mentoring'
     */
    flags: ActivityFlag[];

    /**
     * Предпочитаемый способ связи.
     * Примеры:
     * - 'Email'
     * - 'Telegram'
     * - 'DM on LinkedIn'
     */
    preferredContact?: string;
  }>;

  // ─────────────────────────────────────────────────────────────
  // 8. Контент (статьи, видео, посты)
  // ─────────────────────────────────────────────────────────────
  content: WithActive<{
    /**
     * Базовый URL блога/канала (опционально).
     * Примеры:
     * - 'https://your-blog.dev'
     * - 'https://dev.to/your-username'
     */
    baseUrl?: string;

    /**
     * Последние материалы, которые хочешь подсветить.
     * Можно вручную держать 3–5 штук.
     */
    latest: ContentLink[];
  }>;

  // ─────────────────────────────────────────────────────────────
  // 9. Неформальный блок / fun facts
  // ─────────────────────────────────────────────────────────────
  funFacts: WithActive<{
    /**
     * Список "фанов"/фактов/личного.
     * Примеры:
     * - 'I enjoy refactoring legacy code more than starting from scratch.'
     * - 'I like digging into performance and bundle size.'
     */
    items: string[];
  }>;
}
